<header>
  <header>
  <nav class="navbar navbar-expand-lg" style="background-color: #4689FF">
    <div class="container">
      <a class="navbar-brand" href="/"><%= image_tag 'logo.jpg', size: '120x120' %></a>
        <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li>
            <%= link_to "マイページ", user_path(current_user),class: 'btn btn-sm btn-outline-light mr-2' %>
          </li>
          <li>
            <%= link_to "会員一覧", users_path,class: 'btn btn-sm btn-outline-light mr-2' %>
          </li>
          <li>
            <%= link_to "名盤一覧", master_pieces_path,class: 'btn btn-sm btn-outline-light mr-2' %>
          </li>
          <li>
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete,class: 'btn btn-sm btn-outline-light mr-2' %>
          </li>
        <% else %>
          <li>
            <%= link_to "新規登録", new_user_registration_path,class: 'btn btn-sm btn-outline-light mr-2' %>
          </li>
          <li>
            <%= link_to "ログイン", new_user_session_path,class: 'btn btn-sm btn-outline-light mr-2' %>
          </li>
        <% end %>
        </ul>
    </div>
  </nav>
</header>